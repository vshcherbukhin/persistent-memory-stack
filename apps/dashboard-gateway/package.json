{
  "name": "persistent-memory-dashboard-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Stable localhost dashboard gateway for Persistent Memory update handoff.",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^4.1.9"
  }
}
