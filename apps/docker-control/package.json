{
  "name": "persistent-memory-docker-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tiny security-gated sidecar: the ONLY process with the Docker socket. Exposes bounded list/logs/start/stop/restart for this compose project behind a shared-secret token.",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^4.1.9"
  }
}
